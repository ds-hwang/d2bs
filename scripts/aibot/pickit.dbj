js_strict(true);

include('configuration.dbl');
include('cache.dbl');
include('common.dbl');

function main()
{
	// init the config
	addEventListener('itemaction', function (gid, mode, code, global) {
	});

	addEventListener('golddrop', function (gid, mode) {
	});

	while(true) delay(10000); // low priority delay
}
