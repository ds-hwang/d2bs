js_strict(true);

include('configuration.dbl');
include('cache.dbl');
include('pause.dbl');
include('common.dbl');

function main()
{
	// init the config
	addEventListener('itemaction', itemaction);
	addEventListener('golddrop', golddrop);
	while(true) delay(10000); // low priority delay
}

function itemaction(gid, mode, code, global)
{
}

function golddrop(gid, mode)
{
}
