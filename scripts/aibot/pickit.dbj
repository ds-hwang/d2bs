js_strict(true);

include('configuration.dbl');
include('cache.dbl');
include('pause.dbl');
include('common.dbl');

function main()
{
	// init the config
	addEventListener('itemdrop', itemdrop);
	addEventListener('golddrop', golddrop);
	while(true) delay(10000); // low priority delay
}

function itemdrop(gid, code, x, y, mode)
{
}

function golddrop(gid, x, y, mode)
{
}
