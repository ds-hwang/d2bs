js_strict(true);

include('configuration.dbl');
include('cache.dbl');
include('common.dbl');

function main()
{
	addEventListener('melife', function (hp) {
	});

	addEventListener('memana', function (mp) {
	});

	while(true) delay(10000); // low priority delay
}
